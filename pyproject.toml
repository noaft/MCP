[project]
name = "mcp_test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.26.0",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "uv>=0.10.7",
]
